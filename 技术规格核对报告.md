# 剧本杀AI店员助手 - 技术规格核对报告

> 核对日期：2026-04-30  
> 规格版本：v1.9 (2026-04-29)  
> 实现版本：当前代码库

---

## 一、总体评估

| 维度 | 评分 | 说明 |
|------|------|------|
| 系统架构 | ⭐⭐⭐⭐⭐ | 3服务 Docker Compose 部署，架构清晰 |
| 核心功能 | ⭐⭐⭐⭐ | 三大模块完整实现 |
| 前端交互 | ⭐⭐⭐⭐ | 响应式 + 底部导航，功能完整 |
| 数据库设计 | ⭐⭐⭐ | 核心表结构到位，缺少2个关键表 |
| 规格一致性 | ⭐⭐⭐ | 部分字段名/值不一致 |

**整体完成度：约 85%**

---

## 二、✅ 已完整实现的功能

### 2.1 系统架构 (2.1 节)
- ✅ Next.js 14 前端应用
- ✅ PostgreSQL 16 数据库
- ✅ FastAPI 微服务（DOCX/PDF 解析）
- ✅ Docker Compose 3 服务部署
- ✅ 去掉 Qdrant 向量数据库（符合规格）
- ✅ 去掉 Embedding 模型（符合规格）

### 2.2 核心模块

#### 模块一：剧本检索问答
- ✅ 全本上下文读取（不做向量切块）
- ✅ 流式输出（SSE）
- ✅ 对话历史保留（最近10轮）
- ✅ System Prompt 完整实现（规格4.1节17条要求）
- ✅ 剧本选择下拉
- ✅ 切换剧本时清除对话上下文

#### 模块二：剧本改编工坊
- ✅ 四种改编类型（元素替换/剧情微调/视角扩展/手册改编）
- ✅ 改编分级处理（禁止/需确认/允许）
- ✅ 改编方案预览（step=preview）
- ✅ 全本生成（step=generate）
- ✅ 截断检测（finish_reason === "length"）
- ✅ 敏感本二次确认
- ✅ 改编历史记录

#### 模块三：员工信息管理
- ✅ 员工 CRUD（增删改查）
- ✅ 排班管理（日历视图 + 列表视图）
- ✅ 员工只能查看自己的排班（隐私保护）
- ✅ 管理员可查看/管理所有排班

### 2.3 LLM 配置与调用
- ✅ 双模型策略（主+备）
- ✅ 模型切换 API（GET/PUT）
- ✅ 前端显示当前模型
- ✅ 流式对话 API

### 2.4 并发控制 (2.3 节)
- ✅ 最多 5 并发限制
- ✅ 60 秒排队超时
- ✅ 429 自动重试（5秒后重试1次）

### 2.5 缓存策略 (3.3 节)
- ✅ 本地磁盘缓存 `/data/scripts/{id}/parsed.txt`
- ✅ MD5 校验
- ✅ 缓存写入/读取/删除 API
- ✅ 元数据文件 `meta.json`

### 2.6 对话历史
- ✅ 数据库持久化（chat_history 表）
- ✅ 内存缓存层
- ✅ 对话压缩（>10轮时摘要）
- ✅ 切换剧本时隔离对话上下文

### 2.7 登录认证 (4.5.5 节)
- ✅ HTTPOnly Cookie
- ✅ 7 天有效期
- ✅ 30 分钟无操作自动登出
- ✅ 登出 API
- ✅ 操作日志记录

### 2.8 前端交互 (4.5 节)
- ✅ 响应式布局（Tailwind CSS）
- ✅ 手机端底部导航栏
- ✅ 上传弹窗
- ✅ 流式输出展示（打字机效果）
- ✅ 错误提示

### 2.9 剧本管理
- ✅ 剧本列表页
- ✅ 元数据编辑（作者/类型/难度/人数/幕数/时长）
- ✅ 敏感本标记 + 说明
- ✅ 搜索筛选

---

## 三、⚠️ 部分实现/有差异

### 3.1 模型名称不一致

| 位置 | 规格要求 | 实际代码 |
|------|---------|---------|
| 主模型 | `deepseek-v4-pro` | `deepseek-chat` |
| 备模型 | `deepseek-v4-flash` | `deepseek-reasoner` |
| 默认值 | `deepseek-v4-pro` | `deepseek-chat` |

**影响**：模型能力可能与规格预期不同，需确认实际 API 可用模型。

### 3.2 数据库字段不一致（scripts 表）

| 规格字段 | 实际字段 | 状态 |
|---------|---------|------|
| `name` (VARCHAR 255) | `title` (VARCHAR 200) | ⚠️ 字段名不同 |
| `version` | `status` | ⚠️ 含义不同 |
| `original_file_path` | `file_path` | ⚠️ 字段名不同 |
| `cached_text_path` | `parsed_path` | ⚠️ 字段名不同 |
| `cache_checksum` | `file_md5` | ⚠️ 字段名不同 |
| `cached_at` | ❌ 缺失 | ❌ |

### 3.3 员工角色定义

| 规格要求 | 实际实现 |
|---------|---------|
| `staff` / `admin` | `dm` / `host` / `admin` |

前端显示已适配，但数据库默认值是 `'dm'` 而非 `'staff'`。

### 3.4 schedules 班次定义

| 规格草案 | 实际实现 |
|---------|---------|
| `morning` / `afternoon` / `evening` | `上午` / `下午` / `全天` / `晚班` |

实际实现采用中文，更符合国内使用场景。

---

## 四、❌ 未实现的功能

### 4.1 缺少数据库表初始化

| 表名 | 用途 | 状态 |
|------|------|------|
| `app_settings` | 存储模型配置 | ❌ 未创建 |
| `chat_history` | 对话历史持久化 | ❌ 未创建 |

**影响**：`/api/settings/model` 的 PUT 会失败（表不存在）；对话历史无法持久化。

### 4.2 导出功能不完整

| 功能 | 规格要求 | 实际实现 |
|------|---------|---------|
| DOCX 导出 | ✅ 含水印 | ❌ 仅复制文本 |
| PDF 导出 | ✅ | ❌ 未实现 |
| 导出水印 | ✅ "改编版本 · 仅供内部使用" | ❌ 未实现 |

### 4.3 PWA 配置

| 功能 | 规格要求 | 实际实现 |
|------|---------|---------|
| 添加到主屏幕 | ✅ | ❌ 未实现 |
| 启动画面 | ✅ | ❌ 未实现 |
| 离线提示 | ✅ | ❌ 未实现 |
| Service Worker | ✅ | ❌ 未实现 |

### 4.4 运维功能

| 功能 | 规格要求 | 实际实现 |
|------|---------|---------|
| 磁盘空间告警（80%） | ✅ | ❌ 未实现 |
| 自动备份脚本 | v1 不实现 | ❌ 未实现 |
| 监控部署 | v2 规划 | ❌ 未实现 |

### 4.5 其他 v2 规划项（可忽略）

- 批量导入
- 高频问答缓存
- 问答引用溯源
- 剧本概览卡片
- 流式输出断点续传
- 改编撤销/回退

---

## 五、关键问题与修复建议

### 🔴 P0 - 必须修复

#### 1. 数据库初始化缺少关键表

**问题**：`app_settings` 和 `chat_history` 表未在 `init-db.sql` 中创建。

**修复建议**：在 `init-db.sql` 末尾添加：

```sql
-- 应用设置表
CREATE TABLE IF NOT EXISTS app_settings (
    id VARCHAR(50) PRIMARY KEY DEFAULT 'default',
    active_llm_model VARCHAR(50) DEFAULT 'deepseek-chat',
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- 对话历史表
CREATE TABLE IF NOT EXISTS chat_history (
    id SERIAL PRIMARY KEY,
    session_id VARCHAR(100) NOT NULL,
    script_id VARCHAR(100) NOT NULL,
    role VARCHAR(20) NOT NULL,
    content TEXT NOT NULL,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_chat_session ON chat_history(session_id, script_id);
```

#### 2. 初始化默认设置

```sql
-- 初始化默认模型配置
INSERT INTO app_settings (id, active_llm_model) VALUES ('default', 'deepseek-chat')
ON CONFLICT (id) DO NOTHING;
```

### 🟡 P1 - 建议修复

#### 3. 统一模型名称
确认实际可用的 DeepSeek API 模型名称，或更新代码以匹配规格文档的 `deepseek-v4-pro` / `deepseek-v4-flash`。

#### 4. 字段名一致性
考虑重命名数据库字段以匹配规格文档，或在代码中添加字段映射注释。

### 🟢 P2 - 建议改进

#### 5. 实现 DOCX 导出
使用 `docx` 或 `mammoth` 库实现带水印的 DOCX 导出。

#### 6. 添加 PWA 配置
在 Next.js 中添加 `manifest.json` 和 Service Worker。

---

## 六、功能对照表

### 核心功能对照

| 功能 | 规格章节 | 实现状态 | 代码位置 |
|------|---------|---------|---------|
| 剧本上传 | 5.2 | ✅ | `api/scripts/upload/route.ts` |
| 剧本解析 | 5.2 | ✅ | `fastapi-ai/parse.py` |
| 全本问答 | 4.1 | ✅ | `api/chat/route.ts` |
| 对话历史 | 4.1 | ✅ | `lib/session.ts` |
| 历史压缩 | 3.3 | ✅ | `lib/session.ts` |
| 模型切换 | 2.3 | ⚠️ | `api/settings/model/route.ts` |
| 并发控制 | 2.3 | ✅ | `lib/concurrency.ts` |
| 剧本改编 | 4.2 | ✅ | `api/adaptations/route.ts` |
| 员工管理 | 4.3 | ✅ | `api/employees/route.ts` |
| 排班管理 | 4.3 | ✅ | `api/schedules/route.ts` |
| 登录认证 | 4.5.5 | ✅ | `api/auth/login/route.ts` |
| 操作日志 | 4.2.4 | ✅ | 各 API 路由 |
| 敏感本标记 | 4.2.4 | ✅ | `scripts/page.tsx` |
| 剧本元数据 | 4.2.4 | ✅ | `api/scripts/meta/route.ts` |

### 前端功能对照

| 功能 | 规格章节 | 实现状态 |
|------|---------|---------|
| 响应式布局 | 4.5.1 | ✅ |
| 手机底部导航 | 4.5.2 | ✅ |
| 剧本选择 | 4.5.2 | ✅ |
| 流式输出 | 4.5.3 | ✅ |
| 登录表单 | 4.5.5 | ✅ |
| 剧本管理页 | 4.5 | ✅ |
| 改编工坊页 | 4.5 | ✅ |
| 排班页 | 4.5 | ✅ |
| 员工页 | 4.5 | ✅ |
| PWA 支持 | 4.5.1 | ❌ |

---

## 七、总结

### 完成度评分

| 模块 | 完成度 |
|------|--------|
| 系统架构 | 100% |
| 剧本问答（模块一） | 95% |
| 剧本改编（模块二） | 85% |
| 员工管理（模块三） | 95% |
| 数据库设计 | 75% |
| 前端交互 | 90% |
| 运维功能 | 30% |

### 下一步行动

1. **[P0]** 修复数据库初始化脚本，添加 `app_settings` 和 `chat_history` 表
2. **[P1]** 确认 DeepSeek API 模型名称
3. **[P1]** 统一数据库字段命名
4. **[P2]** 实现 DOCX 导出功能
5. **[P2]** 添加 PWA 配置

---

*报告生成时间：2026-04-30*
